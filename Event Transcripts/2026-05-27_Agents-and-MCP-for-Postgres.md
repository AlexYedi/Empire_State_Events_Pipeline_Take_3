# Agents and MCP for Postgres — Raw Transcript

- **Event:** Agents and MCP for Postgres (NYC Postgres Meetup)
- **Date:** 2026-05-27, ~18:35
- **Location:** Google Chelsea Market, 75 9th Avenue, New York, NY
- **Speaker:** Gleb Otochkin (Cloud Advocate, Databases, Google)
- **Host/Organizer:** Google + NYC Postgres + Mason Sharp (pgEdge)
- **Notion Event:** https://www.notion.so/36bd3699c2db81d894dcedec56361e55
- **Source:** Alex's own recording (phone). Diarization labels are unreliable (split on pauses, not identity) — see conditioning notes in the post-event brief. Verbatim preserved below as the primary source.

---

[Speaker 1 — NYC Postgres organizer, opening community announcements]
So something I want to share first is my first journey at [PGDay?].dev. That's happening last week. It was an amazing time. We did a PG Women breakfast and very specially this year. We are celebrating 30th the year, happy birthday for PostgreSQL, and then I think one thing I really want to call out is Bruce [Momjian], who is at. He actually did a very helpful speech there, talking about what makes PostgreSQL the best database now, is that everyone adopts the attitude of service and finds ways to contribute. And I think it's a great time to acknowledge all those great contributions he has done to the community. And also Jonathan, he is a long-term organizer of many Postgres events, including Postgres Development [Conference]. We had — it was amazing. Really, if you missed it, you missed it. But don't miss the next one. It's going to happen in Montreal next year. And if you missed the event last week, the [PG]Day is coming. That's in June 9th and the very special there is that Stonebraker will give a keynote there. And if you really cannot travel, [it's] too far away for you — New York Postgres Summit is coming too. September 30 to October 2nd. Oh yeah, something special for the Meetup is that we have a [dollar] discount code for everyone attending the Meetup, so don't miss the opportunity. And we are still taking proposals. We actually have our program committee members here. If you want to get some insight of how they review all those proposals, make sure you talk to them and submit. No matter if you are an experienced speaker or a first-time talk — we are more than welcome to submit a proposal. We always, and we really are looking for the entry-level sharing that will inspire everyone to participate. And yeah, as you [know], PostgreSQL actually is a very community-driven community, and all of the work actually is done by people volunteering, participating. We are very honored to have all those long-term contributors to participate, and we're always looking for speakers, sponsors, even coordinators and people who can help us create marketing assets, share on LinkedIn / social. If you are taking a picture today, make sure that you are sharing it somewhere so more people can know that we're having this amazing community and event every other month. Last but not least, we cannot [do this without] the generous sponsor from Google. I really appreciate all those Googlers' support today. And as I'm sure everyone here today is very excited about Gleb's talk. Hey, here comes the lab.

[Speaker 2 — Gleb Otochkin, intro]
Oh, it works nice. We are going to talk about boring stuff. We are going to talk about AI, of course. Everybody has already heard about AI — last year, and every year for the last three years. But what is the main topic of discussion? It's the agent, MCP, and how it works, why we work with Postgres, right? So a couple [of words] about myself, who I am. I live in Ottawa, Canada. I used to be a scientist in the past. I did general expedition Pacific doing some research. And I run a lot. I did Boston Marathon this year. I did a [full] marathon Sunday. Hopefully more marathons coming, so that's about me. And I work at Google as a cloud advocate for databases.

[Speaker 3 — Gleb Otochkin, main talk]
How many of you guys are working with MCP and agents, how familiar [are you] with the concepts? All right, quite a few folks, but not everybody. So I will spend a couple of minutes talking about the basics — what agents [are], what MCP is, how agents work, and what exactly we are trying to do with agents. And then I'm going to talk about agents and Postgres, and how this database can be used with agents and for agents. Then what I would like to do — I'm really biased about some things, we have a lot of different MCP open source tools, and I totally respect that. I tested some of them, but so far my preferable tool is MCP Toolbox. It is developed by my friends at Google. And I'm going to just introduce it as one of examples of open source tools you can use right away out of the box. And then we talk a little bit about remote MCP when you don't need to install your own toolset and use already [available] [...]

[Speaker 3 — Gleb]
Available for some service providers, and a little bit touch probably [natural language] to SQL. And if we have time, I can show a real demo here on my laptop. If we have time, and if you are willing to actually see that — because let's see how it works. Speaking about the questions, if you feel you have questions, if we can keep questions to the end of the talk, it would be great. If you feel that something I said is not correct — which is quite possible, I don't claim to know everything, experienced everything and tested everything — so that's all right, and we can talk about that. And I'm pretty much open to anything back.

So the basics. What is an agent itself? The agent itself is, let's say, this application which can use an LLM to perform a task based on user input. It is using some tools, and the agent uses [the] LLM to decide what tool is used and how the tool is going to be used to achieve the goal. That's essentially a [natural] engine. It is using a model, it has tools, [the] agent itself orchestrates the execution itself. So that's the simplest agent. If you think about any kind of application — Gemini or any other application — it is already [an] agent because behind the scenes they are using tools. For example, they are using Google search or search inside internal database and rules and everything else. For example, the agent — we have a simple staged agent [that] has two tools. One of them is "get the weather", another [is] access to alarm clock or some kind of alarm notification, and I am asking "set my alarm when [it's good for running]". All right, I'm not specifying what kind of weather is right for running. I let the agent to decide what is good. Then [the] agent — "I need to check the [weather] in Ottawa, Canada", getting the weather, decide "oh the weather is sunny and looks like good for running", and it should set [an] alarm. And then I'm getting [an] alarm to my watch, phone, or email. So that's the agent. It is [the] simplest agent, probably.

[Speaker 3 — Gleb]
So what kind of problem [is there]? It works really well when you develop your application, agent, tools and everything else by yourself using the same [framework]. That works really well. But when you have, in [a] framework, you can [be] given tools developed by different people. In that case, your integration points are essentially multiplied — number of agents by number of tools. Even if you have three different agents using three different tools, you have to develop nine integration points. So it is not always the case, simplified, but it is a problem. And Anthropic came to that problem earlier than anybody else, and they proposed a way how to deal with that problem. What they proposed [is] to make some kind of standard — Model Context Protocol. And it is actually what MCP stands for. It is [an] open protocol where one part always knows how to call another part and know what kind of response it is going to get in that particular call. For example, you can issue requests to list your tools to [the] MCP server, and [the] MCP server will return the description and number of tools it has, and the model can be used in execution. So essentially it is kind of — Anthropic proposed a [standard like] USB.

[Speaker 3 — Gleb]
Is probably not exactly the same, but yes. If you have used [USB] on my laptop — I have a USB port and I don't really care what kind of framework, what kind of hardware and software [is] on the other side. As soon as I'm getting [it] resolved, right? That the same applies to MCP and tools and agents. So essentially, [the way] it works — you have [a] host application environment where you have an MCP client. And [the] client [and the] LLM issue requests using JSON-RPC. It could be over HTTP or standard IO. And that request is [served by the] server, [which] in its turn has integration of different tools. And for example, for [a] different RPC call, JSON-RPC call, it [returns a] different response. The workflow essentially is going through, first, this connection [phase]. [...] when, for example, [the] agent going through [the] MCP client is getting the list of the tools and understands what it can do. Next stage, it is request and execution itself. So it is the same — "oh, I have that list of tools", and then I am executing the tools. I know what kind of argument you have to execute for each particular tool, and then you execute those tools and [get the] result in expected format. And it is JSON format, in response for your JSON call. So that's about [agents].

One word about the skills. I know — how familiar are you guys with skills for agents? All right, I think it is the same set of people who are familiar with MCP, right? So the skills [are]... The problem with tools — for example, for GitHub MCP server, you have multiple tools. Each tool returns [a] description and what it can do and [a] set of arguments and parameters. The problem [is] when you have too many tools, your context window is going to be very big. And you are not using all the tools all the time. And the problem with that — your [context], the quality of your agent['s] work will go down. More context window is getting unnecessary information. It['s] called the bloating context window problem. Then, I think it was end of last year or middle of last year, when people came with a solution for that problem. Let's set on skills. And in skills, we provide only [a] very high level of user [journey], and what kind of tools would be suitable for that user journey. In that case, you are not loading all the tools, you are loading only [a] set of [user] journey skills for the agent, and that agent will pull those tools and MCP connections [only] when it is necessary for execution of the [task], and can help to [unload] the block. So that's about agents. I hope you're with me so far. All right. Looks like everything is good.

So a few practical advice[s] from experience working with MCP. What is [the] difference? Some people say MCP is just another version of API. Sometimes it is — sometimes people do it [that way], like another version of API. And I will touch base a little bit [on] cases when it is inevitable. But in reality, what you want from your MCP server is to build [it] around your job. You can have tons of different API calls on your backend. But if [a] user wants [a] report of sales for last month — that report for sales for last month or a particular day interval is [requested] by like 80 percent of your users of your company — it's [a] perfect candidate [to] create [an] MCP tool for them and provide only one parameter, like "from day one [to] day two" is two days, and you are getting sales [data]. And you don't care what kind of [API] calls are going to be executed on backend, or what kind of SQL queries, or maybe a set of different SQL queries, [a] set [of] analytical queries will be executed. [The] user need[s] to get [a] result.

[Speaker 3 — Gleb]
That's your MCP goal. It is providing the tool to the user to get [a] provide[d] input to the agent with a natural language and [get a] result back. That's [the] idea of MCP. The arguments for MCP — the models don't really work really well with dictionary type of arguments. That[']s [what] we [learned] using, [realizing] it by [the] hard way when we tried and then realized "oh, that's not what we [want]." And so make it flat and make [the] least number of arguments as possible. That's the best way how to do that. Name tools appropriately to [the] function they execute — that helps to avoid some problems. If you name [a tool] X1, X1-3-2, [the] model might, even having [a] description, it can sometimes be misled by [the] name and not execute properly, not [be] called when it has to be called. [And] think about [the] context — the description for the tool has to be minimal but provide enough information. [The] name for the tool — again, you don't want to have [a] name for the tool like, I don't know, 2000 characters and everything else. And the reason is quite simple. Every time when you call [the] MCP server, that discovery phase, it will pull all information about [the] tool to the context window. And it will be used in every query [to the] model. And it is tokens you pay for. And even if some information is going to be cached on the model side, you still pay quite a lot if you transferred back and forth unnecessary context. And again, I probably want to iterate once again: MCP is [not] API. API is what your service can do. MCP is what your user wants to do. That's the [...] difference.

[Speaker 3 — Gleb]
Postgres [and] agents, [in] quite a few cases. First, Postgres for agents — how agents itself can use [the] database for [a] system family. You want to store your content [in a] database if you want your engines working reliably. And if you want to use your contents in [the] next iteration with the customers, you can use it for prompt management, for instruction, for guard rails and everything else. Of course you can use [it] for retrieval augmented generation. That is, when you use your data from the company to improve [the] output of your agents. You can use it for database management. You can create [a] database management agent, and it will work. And we have agents which can manage databases. You can use [it] for troubleshooting and monitoring, of course. That's quite a few cases. What I'm going to do — I'm not going to go deep to each case how exactly you want to do that. But for example, when you talk about persistent memory — why you want to [store the] memory of [the] agent in the database? The problem is, when you load all the context, all interactions — and it can be more than one interaction with the customer, client, [or] user with your agent — during all the back and forth, the context is growing. Sometimes you need some information from that context. But what if your agent is crashed? Because it is [in] memory, you lose all [the context], and you have to start from scratch. And if you think from [an] enterprise point of view — do not rely on one computer. Every single machine will break sooner or later. So that is not "if", it is "when". It is why episodic memory and all the interaction[s] with agents [are] probably a good idea to store inside [a] database. What you can do — you can [do] semantic search when you want to retrieve several steps. And understand also why it is useful. [You] pulled a lot of information to context. For example, you have, let's say, 24 different interactions back and forth. There's a lot of context, and not all information is useful, right, for you. But you want to reduce that context to pay less for your tokens. In that case, what you can do — you can compress. [...] What do you mean, compress? First, you create a summary of all interactions, [a] quick summary which compresses all the communication to some main points, and then keep only that — keeping the [latest] interactions inside [the] database. But when you want to retrieve some information which already was deleted during that compression of your context window, then you can use semantic search inside [the] database and get it back to your context window.

And of course, guard rails — all the procedures and everything else can be stored inside [the] database. So it is quite useful. Prompt management — I saw quite a lot of cases when [a] Postgres database was used to store different versions of the prompt for [...] prompt evaluation. So most of the people don't do prompt evaluation. And I think everybody should, because "good enough" sometimes is not good, really. Because [...] it is, "oh, I put that prompt, looks like [it's] working all right", or you change [it a] little bit, that probably provides better output. But are you actually sure that it is providing better output? Have you actually measured what the changes of that prompt return back to you, and what kind of quantitative measurement you did to evaluate that? So in that case, we have, of course, some evaluation services at Google, but we are talking not about Google. We are talking about Postgres and how Postgres can actually save different versions of the prompt, and then you can evaluate each prompt using [an] automated process, and then get some ranking results from the same database and get the best prompt out of that. So it is one of the cases where you can use [the] database for the agent.

When you work with your data — it is, we all know about RAG workflow and how it works, right. It is augmented generation of the answer. So you have your request that an agent is using [an] MCP tool to call to the database, get information — sometimes this [is] proprietary information, sometimes it is numbers, sometimes it can be different cases. Then it is getting that piece of information retrieved from [the] database back to [the] LLM, and then [the] LLM, using that additional information from [the] database, improve[s the] response and provide[s] [it]. That's RAG in [a] nutshell. And when we talk about relational databases — [they] are good because you can use different techniques, combining semantic search [with] a proper query language, combining different filtering and everything else, applying index search and hybrid search all together to get the best results. And of course, yes, it's quite good. In that case you have data search, you have all the data types, you have hybrid search in [the] Postgres database. So in essence you have all the tools in one database. You don't really need more than one in that case. Of course, I'm not talking about some [edge] cases when some specialized database provides unique capabilities you really need in your business. But when you know that — that is a different case — then you combine more than one database for [it].

So again, why [the] Postgres database? It is [a] consolidated data architecture supporting all kind[s] of techniques to provide [a] basic information layer for [the] agent to make them work better and for [the] quality of [the] response itself. And of course, Postgres — always consistent data. Somebody can say "not always" — I know the cases when the data might not be consistent, but in general it is not eventual consistency. It is consistent data, [...] across your [...], based on that vector — everything is together, and everything is consistent. So in my opinion, it is really a good candidate for [an] embodiment of the database which can handle any workload.

Text to SQL. It is another case, and it is quite [an] interesting case, because a lot of people are playing with it, a lot of people are trying to make it working. Sometimes it does, sometimes it doesn't. But it is kind of interesting. I was playing with that a lot lately, and we at Google [are] trying to make some services which are working really well. We just introduced QueryData at Google, and so far it is [a] preview version, and we are trying to understand all the [edge] cases and limitations. But you can have an agent which is working through [the] database, and you can ask [a] natural — using [a] natural language question about your data and get results. It works even out of [the] box sometimes, not reliably, not always, but it works. So if you apply some additional information, it might work even better.

[Speaker 3 — Gleb]
Management [of] databases. I think it is almost [the] last of the slides, and then I will talk a little bit about tools. [In] other case[s], our customers, when they employ — not one agent, sometimes — in most cases, it is more than one agent. It is like a set of different agents which combine the monitoring through MCP and [the] troubleshooting through MCP, providing very useful [...] information for people who are managing databases. And in that case, it can be multiple — all the backup logs, all the database logs, everything is getting back to the agent, and [the] agent summarizing and providing some conditions to understand when [an] alert or email or some information has to be sent to people to [notify] them that something is not right, or something has to be done, or something has to be changed on [the] database or infrastructure.

Tools — how you can [...] and how you start to work with MCP. There are quite a few ways. One way — so I know at least about 20, 20 or 22 different tools providing MCP service for [the] Postgres database. Some of [them] proprietary, some of them open source. I'm biased here. My friends at Google, Curtis and other guys, created — I think it was more than one year ago — they created MCP Toolbox for databases. And they're still working on it every single day. And I think it is [a] great tool, and I want to introduce it here. It is [an] open source tool. It is on GitHub. Everybody is free [to] go grab it and work with it. It has APIs, it has connectivity, authorization, and identification support. It has multi-language SDK[s]. And it works not only with Google databases, of course. It works with Google databases because it is created by [a] Google team, but it works with all other databases as well. It is database agnostic, and we are trying to keep [the] open source spirit of that tool. If people want to work with MongoDB, all right. If people want to work with [Valkey], okay, we [...]. Let's create Oracle — and we have a lot of contributors there as well.

[Speaker 3 — Gleb]
Technically, it works not only with [the] database. It works also with all the different framework[s] and tools for development. It is like Cursor, Claude — Claude desktop, Claude code, Gemini CLI, Visual Studio, and everything else. So this everything is documented. If somebody wants to create an additional piece to [the] framework with [the] tool, everybody is welcome to connect to the team and create an issue in GitHub and work with the team. Definite supported database engines I guess — on Google Cloud databases, MySQL, SQL Server, Postgres, Dataplex, Looker, everything is documented. You can always have a look, check how it works for you, and if it works, use it. Integration with [the] MCP client is quite simple. In reality, it is like you install the tool — whether it is [...] installed locally or installed as a service. Then you provide the parameter to your tool and provide parameter[s] about [the] Postgres instance you have [...] working with. It is host, port, database, user, password — nothing fancy, nothing special. It is actually working in the database, it is connected and working with the database. Here, the GitHub repo and documentation, everything is available all the time. So guys, try to use it and let us know if you want something special or something that's not working for you.

A couple of words — you can install an MCP locally or [as a] service by yourself and manage [it] by yourself. What we have created at Google also — we [are] providing managed MCP for all our databases. And the reason [is] quite simple. If you want your MCP server to be — if you don't want to manage it, you don't want to think about authorization, availability and everything else, and [just] get a service access to your data. It is like, we are providing APIs, and we are providing now MCP as well. And it works out of the box. You can integrate it with Google [authentication] and identification. And also what you can do — you can attach it to Cloud Armor. [What] Cloud Armor is, [is a] protection of your agent from prompt injection, essentially, in a nutshell. And that is important because I can break a normal agent — let's say in three, four different [ways], one by one. And it is not difficult. You can actually ask any of them how to break [an] agent, and all [of] them will help you to do that. So it is — again, it is probably more applicable for people who are already in Google Cloud, who are deploying, for example, a workload to [a] Google Cloud database, like Cloud SQL for Postgres or AlloyDB — AlloyDB['s a] Postgres-compatible database. It is not exactly Postgres, it is [a] Postgres-compatible database, but it provides some quite [a] few cool features inside. So if you have questions, we have tons of already [Googlers] here who are happy to answer any questions. Technically you just enable MCP on your project, configure the client, and it works right away. You can create [a] log table, load data, [load a] schema, run SQL, optimize performance, optimize your instance, and everything else.

Just quick about natural language to SQL. The main problem with natural language to SQL that we found is — it will create any [SQL] — any LLM will create your SQL. The question is, the SQL might not work, because SQL is [a] standard, but [the] implementation on each database engine is different. Also, out of the box, [the] LLM has no idea about what your functions are. It can figure out some tables based on table names, column names and some other stuff, but to understand exactly all [of] your data layout, you have to provide context in some cases. That's one of the challenges, and we are trying different ways how to do that. One of them is what we created — we created an API for conversational [analytics]. It means we provide some kind [of] text about the functions, data, what kind of SQL would be useful based on different intent, to common storage. And then when you ask your database to do something, it is using the semantic search in that storage, applying that information from your input, and understand[ing] if any of [the] available template[s] and context would be applicable to your request, and then using that additional information — and that can be, for example, [a] template for your query using special functions inside, or indexes or anything else, exactly what you want your query to be executed in production. And then it gets that particular information attached to the original request, and then create[s] the [query]. And then you can execute that query. So essentially [it] reduced [the] number of steps which we require — which any model would execute if it doesn't know anything about your data.

So I have some [DM about] Google Cloud here, but — how [are] we about the time, are we all right? Okay. So guys, do you want [a demo] or you want Q&A? Yeah. All right. Yeah, so let's do that.

[Speaker 1 — Gleb, demo narration]
I'm not going to use any kind of recorded demo. I'm going to run [it] on my laptop. If you don't hear me well, what I'm saying, just let me know please, because I'm going to use both my [hands]. All right.

I have a small [virtual] machine right in my cloud. And on that machine, I have installed [the] binding for MCP Toolbox, some configuration files. For example here, the [toolbox] by [nature]. You can see here that is [the] body itself — that toolbox as a tool. Then I have a configuration file for [tools and its] tools [in] yaml.

[Speaker 2 — Gleb, demo]
And what it has inside — if we have a look, it has a data source with my [Postgres] database, and you can see the database is running on the same box, and I have some [details] for [the] database — database, password, and everything else. And [then] I have created some tools, and this small example set of [tools] for search for like [hotel accommodations] in different cities. It is [a] very small data set. But apart from that particular[ar] data set, I created a whole bunch of other tables. And those tables are not related to that particular use case at all, which makes [it] hard [for the] model [...] to understand what exactly is going on. So here, the tools I have — it is, you can book [a] hotel, find [a] hotel, and everything else. So that's [an] example. Then here I am going to use a couple of tools. One of them is going to — first, let's start our MCP Toolbox. All right. Let me quickly [place] what I'm doing. I'm starting with that [...]. So the toolbox has started. So essentially, what we have right now — we have [an] MCP server and [...] two data sets, and each [tool] so far has only [a] regulatory description, what it does, when you first time execute it. And let's — let me start.

And let's say [I'll] show you this curl command, how you can get, for example, [a] list of tools. So you just [...] call using HTTP to that MCP server, and what you do — you provide [a] "tools list", and that is [a] standard call for any MCP server. [...] [It] will respond to that call exactly here.

[Speaker 1 — Gleb, demo]
Oh yeah, of course, I know why, guys. It's because I'm not connected. So let me do it again. So now we're on the same machine, and we [...] [are] [...] [on] that local host. Do you remember, on the integration file, we [...] [had] all those tools configured one by one, and those tools essentially do [things in a] simple way. Also, what we can do is — [the] expected request is to get information about each tool. If you get the [tool], "search hotel by name", it provides quite a bit of information. So that information will be always in your context of your agent [...] if you work directly with [the MCP]. So if you have 140 tools with tons of descriptions [...] it will be quite a bit of context every time when we try to interact with the model. And we can execute the code, for example — we have an argument for "search hotels based on name". And if we [...] — if you have information about [...]. Oh yeah, so here in the description, the [name] for that is [...] input schema is following the parameter, and you have the [right] [...] of the name, right? So, and then you execute the tool, and you provide that argument and name for the [tool], right? That's [the] name for that tool, and [...] from your database here. What we have in our database [...] request. So that's how it works.

Also, if you don't really know what kind of tool you want to [use], then you can use [a] prebuilt set of tools for any [Postgres] database. Essentially, you [are] [...] [running a] simple [...] box [that] provides a [prebuilt] set of tools for [the] Postgres database, [the] MySQL database, any other database. And so what you do — you start with [the] prebuilt parameter here. And then if [we] have a look [at] the tools again — let's [list] all the tools, what we do. [The] data set. And you can see a lot of different tools. I guess you can get all the settings, you can [...] [show] them [...]. And each tool has its own set of arguments. And the same way as we wrote [a] set of arguments for [...] [those tools] by name, we can [...] — we can [show], for example, what kind of argument. Right? Everybody can guess what kind of [...] the SQL itself. So yes, that is simple to execute. That's one argument you have to [provide]. And of course you can provide — you can just run that curl command and just select [...]. Now it is probably the easiest [thing] we can [do]. And it will provide you now [...] just the same as you run that [...]. It is, right? So that's how it works in [a] nutshell.

How it can work with natural language requests, so let's try. And one — [I'll] indicate [an] agent. So what [I] [...] [stuck] in [...] cure — I'm [...] [a] small agent. I just created [it] using Python. It is [a] small agent. It has very little description about it. [What it] can do — it is just [a] basic agent. You can work with [the] database, and what it has — it has information how to connect to that MCP server. I started another window. That's it. I'm using here. So let me make a difference. So here, my agent. Let's check it before. It['s] [...] it is ADK. It is agent development [kit]. It is [a] framework to create [an] agent, and as a framework that provides [a] web interface, and [a] web interface with space. So what I can do here? It's always [...] really difficult to work with [...] [it]. I just say "hello". What it does behind the [scenes] — it is, okay, "[tell] me [...] who I am, what they can do", and then [...]. Oh, "I'm your database assistant, because [it] includes — I have [...] [a tool] set, and I [...] [understand] — oh, probably I can answer to some database question[s]." That is [a] guess from [the] LLM — [the] LLM token[s]. But what they can ask? "Can you show me [...] information." And then eventually it will execute the code and provide the same information as we call.

[Speaker 3 — Gleb]
For example, [it's] [...] [using a] specialized tool from MCP [Toolbox], right? So that — but how many calls did we do? 18 calls to get information.

[Speaker 1 — Gleb]
And each call [costs] you [...] [time], [...] [it's your] responsib[ility]. So what we can do? Let's go [to] "all agents", and here the tokens we spend — 62,000 [tokens for that] in total for all our requests. If we do the same, but when we have [the] toolbox [...] integrate[d] with tools based on [the user] journey — let's do it again. Say "hello", it will get the same as the [...] discussion. What type of tools and everything else? And then you can see that [...] here. "I'm a database assistant." Yeah, it can understand what it can do. Then "can you [...] information." The number of [calls] [...] [held] down. Let's see how much happens. And if you multiply it to multiple calls from different [users] and everything else, you can see that [...] MCP should be your user journey, not just [an] API call. That's [the] idea. So the question was, what can [...] — so that, if you find it, they don't remember. So, you guys [...]. Oh yeah. That's only that part. So it is why I say that [the] LLM can be tend to [...] [hallucinate / get it wrong]. But — and it is using only, for example, because it took only eight steps more to understand that [the table] contains information you are asking, and it is normally [...] [worse]. The model is, I believe it is — prevent[s] the model from [...]

[Speaker 2 — audience question, paraphrased by Gleb]
Running [a] delete, like a delete [...].

[Speaker 1 — Gleb]
All right, the question is — let me repeat the question first, and to understand if I got it right. And before doing that, let me — let us switch to full Q&A [mode]. Let me finish [...] some presentation stuff, right. [...] [The] demo, let's [...] [show]. No, I don't want it. So a couple of things at the end — what you can use, where you can start. You can use, for example, some tools like Codex, or anything else — Antigravity right now [...] [you] really need [to] study [the] varieties [you'd] like. [These] two with [a] managed MCP server. [...] If you work [in] the cloud, if you work with your local database or on-prem databases, [use] [...] [Toolbox] or any other open source tool again. By [default], [with] any tool like Antigravity or any other agentic tool, it works really well. Actually, I do it all the time. And AI Studio — if you want [a] web experience, essentially. That's how you can start and how you can really play with it. There are some resources. We try to publish all the [...], all we try [to] combine some resources about Google and [...] stuff. I have some [codelab] created about natural language and how to start to work with QueryData. If you want, you can try it and let us know whether you want more information, [or] maybe it's something that's not correct, something is not working for you. I'm happy for anything. And thank you very much.

[Q&A]

[Speaker 1 — Gleb, answering: how to prevent delete/harm by an agent]
How [do] we prevent delete or harm made by [an] agent, right? Yeah, how do you remove that kind of possible[ity]? So there are a few ways to do that. You can do it on [the] backend — when you develop your MCP server, do not provide [a] tool which can delete or update any data. You can create read-only access. The second is, you can do it on [the] database level — whether it is [a] managed database or it is Postgres — based on roles available for [the] user which is working, connecting as [the] MCP server to your database.

[Speaker 1 — Gleb, answering: the 18-calls vs 8-calls difference]
So the question is — what['s] the difference between [the] first run with 18 calls to [the] model and eight calls to the model, right? The reason why the second time it was only [...] — I'll be brief — [fewer] calls to the model, [is] because we have already [built tools] based on [the] user journey. So what [the] agent did — only it [...]: "oh, I have [a tool that] perfectly fit[s] to my user request. I'm not going to search tables, I already know how to execute that query out of that tool." It is why MCP is not [an] API. There [is] — by the way, I promised, I would say something about why sometimes MCP looks like [an] API. The reason — for example, at Google, we created [an] MCP server, managed MCP server for [the] Cloud SQL database for Postgres. Do we know about each user journey? Of course we don't. And in that case, you don't have too much choice but [to] create [an] MCP server which probably can help with basic stuff like execute query, understand[ing], troubleshoot[ing] [the] database, manage[ing] [the] database instance, and everything else. It is not exactly [an] API, because behind each tool there are multiple API calls. But it [...] looks like [a] universal API — different layer of API. That's why sometimes you just don't know what kind of user journey is going to be [needed].

[Speaker 1 — Gleb, answering: row-level security]
Yeah, it makes sense. So the question — how [do] we handle row level security? That is [a] question probably not for MCP, but what kind of access [the] MCP server has, right. There are two ways how to handle [it]. One way is — you provide a role-level access. You can do it in Postgres. [Is] it ideal? Probably not. Another way how to do that is [to] [restrict access] to the schemas. Another way, it is authorization. Another way — we have created [for] Google AlloyDB special[ly] — we have [parameterized] security views, and those [parameterized] security views — you cannot access [a] table ever, but you can call [a] procedure which provide[s] you access on [a] role level. That's another way how to do that. But it is actually [an] open question. Security with MCP is [a] very big question, and it is very individual for every single case. But yes, you can try different ways. Whether it is the best way ever for everybody — probably not.

[Speaker — audience question on fast tools / response time]
When it comes to making a fast tool [...] [different tools] take different amounts of time [...] [any] advice on the complexities, like for example [...] [a complex] database and you just [...] [no] feedback you have on how to optimize [...].

[Speaker 1 — Gleb, answering: response time for MCP tools]
The question [is] about the response time essentially for MCP tools, right. And the answer [...] [first] — I would say this is my personal opinion: MCP is not for OLTP. You deal with multiple different layers. And you probably need to understand what kind of user requirement[s there are] for MCP. In my experience, what people are using [it] for is for management [of] databases, for analytic queries, where [you] call for [an] individual [...] [query] [...] the overhead is negligible for [the] length of [the query] itself. So that's the way. But [in the end], it depends what kind of tool, what kind of user journey, what kind of business requirement. There [is] no universal answer for that. But of course, if your tool can work faster — what you can do, you can, for example, build [it] [in] the most efficient way behind [the] [...] [tool], right.

[Speaker 1 — Gleb, answering: should we give the agent free hand to decide?]
Sorry, [the] question is — the prompt was very simple and rudimentary, and whether I think that we should give [a] free hand to [the] agent to decide what and how to execute. Yes and no. I would say it depends on [the] business case, and I think a good prompt for [an] agent cost[s] a lot of tokens at the end. And when you do that — again, you can manage the system prompt with system instruction, different level[s] of prompting. Some of them will be cached on [the] model side. You need to understand how each model works, what information can be cached on [the] backend and not consume tokens. [There's] some information [...]. Yes, Gemini is cached information, and ADK helps with that as well.

[Speaker 2 — audience question: how does the agent know which tool/query is most optimal without memory?]
So you figure[d] [...] offering an endpoint to get a list of hotels — and it's not optimized — and then you have another [...] if you're a generic [...] SQL [...] tool, [an] optimized [...] [batch] for like getting [hotels] — like, how does it know which tool is the more optimal way to do it without memory? [...] You can also get a list of hotels, but you offer an endpoint [to] get [a] list of hotels but it's not optimized like before or something, and the other [...] SQL statement is a batch query for hotels, right? Like, how do you know — how does it know which query is the most [optimal] without memory?

[Speaker 1 — Gleb, answering]
How do you know — I just test the query and put the most optimal query for my tool.

[Speaker 2 — audience follow-up]
Okay, so [...] which is [...].

[Speaker 1 — Gleb]
So the question is how it works [the] most optimal way, essentially, right. So when the agent itself [reads] the tool — at least [the] description for the tool and what they can do — the agent itself doesn't know what kind of query is going to be executed. It just executes [the tool]. In the case when [...] we're going through [a] generic list of execution, [it] just execute[s] the SQL statement, it decide[s] by itself how to execute the query. And sometimes it is not optimal at all.

[Speaker 2 — audience]
[Multiple tools with] similar descriptions to get lots of hotels — [the model] doesn't know which one [...]. Oh — that, you need [to] design your tool to prevent that from happening. Yeah, hold on. [...] [What if] it will just execute both of them and see [which is] going to give a better response? [...] You can do that, but you have to program it.

[Speaker 1 — Gleb]
So one of the suggestion[s] was to execute both tools and see which one is better. It is possible, but you have to program [it], and you need to understand whether you want to do that.

[Speaker 2 — audience]
It's supposed to do it on its own, sometimes.

[Speaker 1 — Gleb]
Sometimes [...] when [does] it [do] it on its own? It is when you don't have that tool for your user journey. Right, okay. So the question [is] whether we can get rid of JSON and replace it by something more effective, [a more] compact way how to pass information back and forth. You can, but in that case we are not using MCP — yes, essentially. So let me get [...] back. MCP can work [in] different ways, but the standard itself [was] developed for RPC [over] JSON interactions. You can do [otherwise] — I don't say you cannot do that with [an] agent. You can use skills when you [...] [define] everything, but in the skills you have to define everything — not only tools, but also a communication and everything else. And that is totally normal. People do that and use not-JSON interactions. And it saves [tokens], that's for sure. Did they answer the question?

[Speaker 1 — Gleb, answering: does ADK need special prompting?]
The question — with the ADK, [does it] have [to be] special prompting? No, it is not about the prompting. It is just — it is about skills. So ADK supports MCP and skills. It is — ADK can do that for you, but you have to define what you have to do. My colleague Carl [...] [wrote a] really good blog, probably like three or four months ago, when he replaced the JSON [...] interaction with [...] [the MCP server], [in the] model, by [a] different format. I don't remember the blog name, but you can search it on, probably, Medium. Yeah.

[Speaker — audience question: how do you scale tool configuration if it's so user-prompt-specific?]
My question is — [the] configuration just [...] [demoed] feels super specific to the user prompt or user request. Since we cannot predict everything that they can ask, how do we intelligently scale the tool?

[Speaker 1 — Gleb, answering]
So the question is — the [tool] configuration was specific for user journeys, right, and how [can] we create [a] universal configuration? I showed [a] universal configuration with prebuilt tools, which can execute different tools like "execute statement" or "list logs" inside your database, or [set] parameters [or] do some other stuff. That is one of the way[s] you can start. But when you finish, it depends on how you use your database. Essentially, it is like — let me be [frank] — can I create [a] universal SQL query which returns optimal result[s] every time? It is the same about tools here.

[Speaker — audience question: were the demo tools part of Google's MCP server or standalone?]
Sorry, if [...] [in] the demo, were the tools you showed in the demo — are those part of [the] Google MCP server, or were those tools being [built] for the demo[?]

[Speaker 1 — Gleb, answering]
The question was — was it about Google['s] MCP service, [like] MCP as a service [from] Google, or was it standalone MCP Toolbox, right. Yeah. So that was [a] standard Postgres database [...] and Toolbox. No Google services were involved.

[Speaker 2 — audience question: how does MCP for Postgres compare to MCP for MongoDB / other DBs? Use cases?]
Have you [...] [had] [a] chance to compare this to MCP for like MongoDB, or like MCP for [...] [other things]? Like, what are the use cases for MCP for Postgres versus those other databases?

[Speaker 1 — Gleb, answering]
The question [is] whether I was able to compare MCP services for Postgres and MCP services for [a] different database engine, yeah. Again — MCP is about user journeys. It means you define the task, you define the tool and arguments for the tool, [and] how it is executed on [the] backend, it depends on [the] database engine and everything else. Whether [it's] effective — sometimes yes, sometimes not. It depends on each particular case. If we talk about the MCP tools in general, like — I don't know what my user journey [is] and here['s] the list of MCP tools — then you can probably compare whether it is sufficient or insufficient or it [...] fulfilled your requirements. But there [is] no universal answer for that. I don't really think I can say that MCP servers in general for [the] Postgres database are better than MCP servers in general for MongoDB.

[Speaker 3 — audience follow-up]
[...] [What] use case [is it] meant to be — for Postgres, [where it] would be better than, like [...].

[Speaker 1 — Gleb]
It is not about MCP for both [...]. [It's about] whether your Postgres database[is the] right database for you, or [a] MongoDB['s the] right database. What is the best [...].

[Speaker — audience question: best way to handle access management / auth from MCP to DB]
[...] [for] access management — when you're trying to grant access to the [agent / LLM] or to a database, is it [a true / dedicated] user account, or are there better ways to handle that?

[Speaker 1 — Gleb, answering]
So the question is, what['s] the best way to provide authorization and identification from MCP to [the] database. I don't really know the answer [definitively]. From my point of view, it depends [on] what kind of authorization you support on your database and application layout — whether it is Active Directory, whether it is Google IAM, [or the] database username and password (probably the least secure way — that's my opinion about that). But take it with [a] grain of salt, it is my opinion.

[Speaker 4 — audience question on transactions]
So it appears that [...] transaction[s] [...] [require] [...] a lot more validations. You can't probably use a regular transaction API to directly [drive an] MCP server [...].

[Speaker 2 — audience question: are Google Cloud customers connecting MCP directly to the DB in production? what use cases?]
[...] [In] your experience working with Google Cloud customers, are there any customers who have started using MCP directly connected to the database in their use cases? And [...] what kind of use cases are you seeing when [...] somebody decided to straight away expose [...] [the] database [via an] MCP server to a specific business outcome?

[Speaker 1 — Gleb, answering]
So the question is about business cases — [does] MCP replace API calls? Yeah. So I will say MCP is not [a] replacement for API calls. It is [a] different purpose. In some cases you can — if you have a management layer for your database engine, you can use MCP [and it] might be much more effective, because MCP works with your LLM agent. But you can replace it by skills with API calls. Which one is more effective? According to my [...] — skills plus MCP is the most effective way; skills without MCP [is] less effective; and MCP without skills is less effective as well.

[Speaker 5 — audience / colleague]
Somebody has done [...] — I don't know the level of production, but we were working with customers. I personally was working with customers implementing — it is like [a] pre-production case, sure.

[Speaker 1 — Gleb]
But yes [...].

[Speaker 5]
No, no transaction case [...] [for high] quantity [...] — you want your application to be performant, and transaction[s] in milliseconds, right.

[Speaker 1 — Gleb]
MCP is not for that, all right.

[Speaker 1 — Gleb, answering: quantified text-to-SQL accuracy data?]
The question is whether [I] have quantified statistical data [on] whether the natural language query is translated correctly to [a] SQL statement. I don't — I have some data which is anecdotal and based on my own [tests]. I cannot [claim] — I cannot claim that number. I don't think it is good enough [to cite].

[Closing]
[Speaker 5] That's got to be one of the best [talks / spreads] I've ever seen [...].
[Speaker 3] Yeah, yeah.
[Speaker 1 — Gleb] [...] Google does it. That's good. Yeah, have a good one.
