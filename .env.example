# HubSpot Private App access token.
# Create at: HubSpot account → Settings → Integrations → Private Apps → Create private app.
# Required scopes (mirror your existing Claude.ai connector permissions):
#   crm.objects.contacts.read, crm.objects.contacts.write
#   crm.objects.companies.read, crm.objects.companies.write
#   crm.objects.deals.read, crm.objects.deals.write
#   crm.objects.notes.read (NOTE: HubSpot calls these "engagements")
#   crm.objects.owners.read
# After creating, copy the Access Token from the Auth tab and paste below.
HUBSPOT_PRIVATE_APP_TOKEN=

# Notion uses the hosted MCP at https://mcp.notion.com/mcp via OAuth — no token needed here.
# Authenticate by running /mcp in Claude Code after the .mcp.json is in place.

# PostHog — dedicated Empire State project (id 524367), NOT gtm_os.
# Capture path (telemetry hooks: build-session-emit.sh, emit-judge-runs.sh) uses the PROJECT_TOKEN —
# the public `phc_` write key (Settings → Project → Project API Key).
POSTHOG_PROJECT_TOKEN=
# Read/query path (used by the empire-state-hub dashboard, not this repo) — kept here for parity:
POSTHOG_PERSONAL_API_KEY=   # personal `phx_` key, needs query:read scope
POSTHOG_PROJECT_API_KEY=    # project-secret `phs_` key (read fallback)
POSTHOG_PROJECT_ID=524367
POSTHOG_HOST=https://us.posthog.com
