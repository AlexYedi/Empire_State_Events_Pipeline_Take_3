# Correction-recurrence log

Append-only. Tracks **recurring corrections** (the same class of mistake across builds) surfaced in the weekly `/rigor-review`. A class hitting threshold (≥ N) triggers a **proposed** codified fix (rubric / DoD / skill / few-shot) → Alex approves → applied (versioned). This is the feedback→improvement channel that makes corrections *stick* instead of evaporating (the root-cause the measurement layer cures).

Format: `- {YYYY-MM-DD} | class: {short label} | count: {n} | action: {watch | proposed: <fix> | applied: <fix + version>}`

## Entries

### 2026-07-15 — first `/rigor-review` (window: 2026-07-08 → 2026-07-15)
- 2026-07-15 | class: thin-declarative-command (a `.claude/commands/*.md` names real agents but ships no orchestration — no dispatch order, no parallel/serial control flow, no output destination) | count: 5 (run-market-landscape-study, analyze-competitive-landscape, create-messaging-brief, generate-channel-copy, test-and-report — all built Jul-2 in one sitting) | action: **applied** (2026-07-15, Alex go) — (1) codified `.claude/references/command-orchestration-convention.md` (the required 7-step skeleton, grounded in interview-prep/event-deep-research); (2) `build-quality@2` completeness anchor caps a skeleton-less command ≤0.35; (3) all 5 commands rebuilt to the convention (test-and-report grounded as the copy-chain's experiment-design link rather than deleted). Re-judged on Haiku vs `@2` to verify. Judge detection was correct; the recurrence was in the *builder*, now fenced by convention + rubric.
- 2026-07-15 | class: dangling-reference-in-skill (a SKILL.md references a repo file that was never created — trend-radar → `.claude/references/signal-taxonomy.md`) | count: 1 | action: **applied** (2026-07-15, Alex go) — created `.claude/references/signal-taxonomy.md` (14-entry canonical map, promoted from trend-radar's inline seed), rewired trend-radar Step 2 to read it (normalization now persistent, not ad-hoc) + added a Step 5.5 pre-flight `SUPABASE_API_KEY` check; codified the general rule as `build-quality@2` (a load-bearing referenced-but-absent file caps completeness ≤0.60). Surfaced by the one judge–human disagree this batch (Alex: judge too lenient on missing references).
- 2026-07-15 | class: judge-item-waived-advisory (DoD item 4 waived because the judge is pre-calibration / new skills need a fresh session to register) | count: 2 (sessions 9523d6a1, aa0f8ce9) | action: **watch** — self-resolving; calibration now 15/20 acked runs @ 93.3% agreement, closing on the ≥20-run gate that retires this waiver reason.

### 2026-07-16 — context-grounded orphan re-judge + a caught defect (mid-session addendum)
- 2026-07-16 | class: unverified-claim-presented-as-verified (a confidence-honesty violation — asserting an API as fact inside a "verified 2026" section without having verified it) | count: 1 (multi-agent-orchestration/SKILL.md → `create_supervisor`/`langgraph-supervisor`) | action: **applied + resolved** — The context-grounded judge (jb-ctx-4, 0.72) flagged it; Alex acked **disagree/should-flag**. That forced a live-doc verification: the package IS real (PyPI + LangChain reference docs), so the claim wasn't false — it was *unverified-when-asserted*. Fix: corrected the file to **cite the source + add the maintainers'-now-favor-direct-tools nuance** (not a hedge — a confirmation). Blast radius traced = 1 file, 2 lines; **no downstream contamination** (nothing built on it; not in the Empire State pipeline). **This is the judge+HITL loop working as designed** — the disagree caught a real honesty gap even though the underlying fact was true, and verifying-before-sweeping avoided a wasted correction pass. Candidate for `build-quality@3`: a correctness cap for "API/claim asserted as verified without a citation."

### 2026-07-17 — Cross-provider judge Approach-A backfill (Gemini 3.1-pro vs the labeled corpus)
Built `.claude/hooks/gemini-judge.sh` (spec: `.claude/references/cross-provider-judge.md`); ran `gemini-pro-latest`
(→`gemini-3.1-pro-preview`) on 18 labeled artifact-states. **Gemini-vs-Alex 15/18 = 83.3%; Gemini-vs-Haiku 17/18 = 94.4%** — the independent provider clears the ≥80% bar and is highly consistent with the Claude judge. The 3 disagreements each taught something:
- 2026-07-17 | class: gemini-false-flag-from-missing-house-context (convention criterion) | count: 1 (bf03 interview-prep-dossier: Gemini flagged 0.63 vs Haiku+Alex 0.95) | action: **validates the scoped quorum** — Gemini mis-read "Postgres deferred" (documented≠executed) AND wrongly demanded an `alex:` prefix on a *project* skill (that prefix is for plugin skills only). Both are house-context errors → Gemini's `convention_adherence`/`anti_pattern` vote stays **advisory**; Claude retains primary. Human tiebreak correctly kept the pass.
- 2026-07-17 | class: dangling-reference-cap-under-applied-by-models | count: 1 (bf17 trend-radar pre-fix: Alex→flag, but BOTH Haiku 0.76 and Gemini 0.89 passed — even with the cap stated in context) | action: **proposed — mechanize the check**: the judge should *programmatically verify referenced files exist* rather than trust the model to apply the `build-quality@2` dangling-ref cap. Models under-enforce human-authored strictness.
- 2026-07-17 | class: confidence-honesty-violation-under-weighted (RUBRIC finding, two-provider corroborated) | count: 1 (bf18 multi-agent pre-fix: BOTH judges CAUGHT the unverified-`create_supervisor`-as-verified defect — Gemini docked diagnostics to 0.0 with exact reasoning, Haiku docked correctness/anti-pattern — but the weighting diluted a single-criterion honesty violation so the *composite* still passed, 0.9/0.72, while Alex wanted flag) | action: **PROPOSED `build-quality@3` hard cap** — an "unverified claim asserted as verified" (confidence-honesty violation) caps the composite below the 0.70 pass band regardless of which criterion it lands in. Strong evidence: two independent providers both detected the defect; only the composite math let it pass. → Alex approve before applying.
- **Meta:** Gemini-3.1-pro scores **bimodally** — compresses good artifacts to 1.0 (13/15 passes) and drops hard on real defects (0.32, 0.63) — less mid-range granularity than Haiku (0.81–0.95). Fine for a pass/flag *gate*; coarser for fine-grained scoring.
- **Provenance:** these are `calibration_set:backfill` (correlated corpus). Approach B (prospective, held-out) is what retires "provisional" — not yet run.

**State:** rigor **holding** — the loop is closed and has now run once end-to-end. DoD writer confirmed (2 non-null `{dod_met,dod_waived,correction_rounds}` triplets vs 0 of 130+ before). Judge–human agreement 93.3% (≥80%). One real recurrence (thin commands, count 5) surfaced and proposed; two watch-list classes logged. No manufactured work.

### 2026-09-18 — second `/rigor-review` (YED-189; window 2026-07-18 → 2026-09-18, the first run in 9 weeks)
The loop itself had drifted: no entry for 9 weeks, while six registry metrics route to this review. Recurrence threshold set to **N = 3** (was an unset seed; see `value-action-registry.md`).
- 2026-09-18 | class: judge-item-waived-advisory | count: **10** (8 since the 07-15 count of 2). Recounted honestly: 13 regex hits − 3 false matches on spec/Linear items. Sub-reasons: advisory/self-produced ×4 (08-05, 08-06, 09-12, 09-18) · deferred to a later run ×3 (07-11, 08-11, 09-11) · fresh-session/registry ×2 (07-11, 09-08) · not a gradable artifact ×1 (07-30, legitimate) | action: **proposed — decision YED-201 (Fix 1A)**. The 07-15 "self-resolving" call was wrong. The pre-calibration reason did end on 07-17, but the waiver survived under a new reason: "advisory tier" came to mean *don't run* instead of *don't gate*. Redemptions: 07-11 → the calibration batch · 09-11 → same session (386be67; weak quorum, no Gemini evidence parity) · 09-08 → partial (v2.2 judged; v2.3/v2.4 never judged; YED-190).
- 2026-09-18 | class: superseded-decision-not-propagated (a tombstone recorded in the canonical spec but not in the files that consume it) | count: **6 files** (pre-event-content, content-correspondent, pattern-synthesis, analyze-competitive-landscape, run-market-landscape-study, evergreen-deep-dive still treat Gamma, removed 08-07, as the live default) | action: cleanup = **YED-200**; codified fix **proposed — YED-201 (Fix 2A: a mechanized tombstone check, the same move as the dangling-ref check)**. Surfaced by the YED-190 judge redemption.
- 2026-09-18 | class: gemini-seat-ceiling (the Gemini seat returns 1.0 whether or not the artifact has a defect) | count: **3** (missed `spine_client` 09-13, `inbox_boundary` 09-13, `pre-event-content` 09-18; Sonnet caught all three; 17 of 18 prospective non-control Gemini runs scored 1.0) | action: **watch → propose at the next review** if it holds. The 07-17 "bimodal" note understated it: on the three artifacts Sonnet flagged this window, Gemini passed all three at 1.0 (Alex confirmed the flag on `spine_client`; `inbox_boundary` was never acked; `pre-event-content` awaits the YED-190 tiebreak), and the only low score it gave was the synthetic negative control. For now, read a quorum "agree" as Sonnet's signal. The 3rd miss was on `correctness`, a criterion where Gemini carries *full* weight, so this is not only the known house-context blind spot. Candidate fix: few-shot real-defect anchors in `gemini-judge.sh`, or demote Gemini to advisory on all criteria until its prospective defect recall > 0.
- 2026-09-18 | class: worktree-false-dangling-ref (gitignored private refs are absent in a worktree, so `check-refs` caps both seats) | count: 1 (role-radar + inbox-miner, today) | action: **recorded** in `platform-constraints.md`. Never accept a cap caused by it.
- **Metrics vs the registry:** judge–human agreement 9/9 acked quorums (100%, but see the Gemini ceiling; 6 of 15 quorums still unacked) → provisional-trusted stays · DoD waiver rate W35 6 → W37 **9** → W38 3 (partial week): the W37 spike clusters on PRD/spec items, which YED-129 (infra = spec, ruled today) should cut · corrective rounds: 3 non-null values in the window, too thin to trend · telemetry healthy (DoD triplets present on 14 of 16 closed sessions; last row 09-17) · outcomes: not reviewed this run (no `/tag-outcome` since the triage).
- **Container-rule audit (Step 2.5):** **35 raw hits → 31 after fixes, all 31 noise, 0 live without a home.** Fixed 4: spine v2 extensions → YED-179 · WORKFLOWS + post-event-synthesis → YED-198 · pattern-synthesis "N-event, not yet built" reworded as out of scope. **Baseline noise floor = 31**; a count above 31, or any new live hit, is the signal.

- 2026-09-19 | addendum (YED-190 redemption runs, Alex acked all 3 splits): gemini-seat-ceiling **count 3 confirmed** (+1 unacked) (`spine_client`, `pre-event-content`, `role-radar` v2.4 passed at 1.0; Alex confirmed the flag on each; `inbox_boundary` still unacked) | **but** inbox-miner reversed the pattern: Gemini flagged 0.66 on a real defect (L99's false "keeps bodies out of the parent" claim) that Sonnet passed at 0.80, while Sonnet caught a different real defect (09-18 rulings not wired). Per-seat vs Alex this run: Sonnet 2/3, Gemini 1/3. **Reading:** the seats catch different defect types; Gemini is badly calibrated (1.0 default), not signal-free. Count ≥ N=3 → **folded into YED-201 as a third proposal candidate at the next review** (candidate: few-shot real-defect anchors so Gemini's 1.0 isn't the default; keep the quorum). Fixes filed: YED-200 (pre-event-content), YED-202 (role-radar), YED-203 (inbox-miner).

**State:** rigor **drifting → recovering.** The loop was dark for 9 weeks. The two proposed fixes are the codified answer (YED-201). The judge's second seat is less informative than its agreement rate suggests.

### 2026-09-19 — YED-201 ruled (Alex: approve 1A + 2A) → applied
- 2026-09-19 | class: judge-item-waived-advisory | count: 10 | action: **applied: Fix 1A.** `dod-close.sh` now rejects a DoD item-4 waiver unless it says *no gradable artifact* or names a YED-/GTM- make-up issue. Self-test: 2 old-style reasons rejected (exit 2), 3 valid forms accepted. CLAUDE.md DoD item 4 and `/dod-close` reworded: N = the build's own session; "advisory" means the verdict doesn't gate, not that running the judge is optional.
- 2026-09-19 | class: superseded-decision-not-propagated | count: 6 → **71 lines / 20 files** once mechanized (after two judge rounds tightened the checker) | action: **applied: Fix 2A.** Tombstones table added to `platform-constraints.md` (one row per removed tool/decision) + `.claude/hooks/check-tombstones.py` (`--artifact` for judge Step 0, both seats; `--all` for rigor-review Step 2.5). No rubric bump (`@4` unchanged). The checker found 9 more files than the hand grep, including `post-event-content.md`, `command-orchestration-convention.md` (the template new commands copy, so it keeps spreading the stale Gamma default), and a Granola MCP call in `transcript-analysis`. Cleanup = YED-200. Its acceptance is now `check-tombstones.py --all` → 0.
- 2026-09-19 | self-judge of the YED-201 build (the new rule applied to itself) | Sonnet flagged both artifacts (0.50 / 0.51) with **three reproduced bypasses**: (1) a `4: advisory…` label skipped the judge rule; (2) an ID inside the label satisfied it; (3) the checker's "any not/no/don't on the line" marker silently cleared live violations (`evergreen-deep-dive.md:61`, `analyze-competitive-landscape.md:50`, `pre-event-content:508`). Gemini passed `dod-close.sh` at 1.0 and flagged `check-tombstones.py` only for a dead skip-list path (fixed). **All three fixed** (label trigger includes a bare `4`; split on the first colon outside parentheses; marker must be within 40 chars; patterns case-insensitive). 11/11 dod-close cases pass, and all 3 missed violations are now flagged. Docs walked back from "rejects any other" to what the code guarantees. Another data point for `gemini-seat-ceiling`: Gemini missed all three bypasses.
- 2026-09-19 | YED-201 round 2 | Sonnet: all 3 round-1 bypasses verified closed; found 7 narrower ones. `dod-close`: an unlabeled reason fell back to "ID anywhere"; full-width colon; colon inside `[...]`; "item four". Checker: re.I flagged ML "gamma"; a marker could come from a neighbouring sentence; bare "gtm-os" is also the live Linear team. Gemini round 2: dod-close 1.0 (missed all four again); checker 0.66 for `--all` being `.md`-only (fair, fixed: code + config now scanned). **All fixed;** 18-case dod-close suite and a 10-line checker fixture pass (4 true hits, 0 false positives). Documented limit: the rule checks the waiver's format, not that the named issue is the real make-up run. Final baseline **71 / 20**.
- 2026-09-19 | bug found while logging YED-201 round 2 | `quorum-merge.sh` wrote the quorum record with `>` to a round-less filename, so a same-day re-judge silently **overwrote** the earlier round's merged record (seat logs survived as separate files). Fixed to append (`>>`); round-1 records for check-tombstones and dod-close re-merged from their seat logs. Alex acked both dod-close escalations as flag. Count 1: watch.

### 2026-09-19 — the three judge-flagged skills fixed (YED-200 / 202 / 203)
- 2026-09-19 | class: superseded-decision-not-propagated | count: 71 lines / 20 files → **0** | action: **resolved (YED-200).** Four content skills and four commands now point at `visual-briefs.md` `## Execution` and no longer restate render mechanics; restating them is how the drift started. 145 dead lines of Gamma/Canva MCP mechanics deleted from `visual-briefs.md`, which had flagged them for deletion itself. The command-orchestration *template* is fixed, so new commands stop inheriting the stale default. The Granola path is labelled disabled, not deleted, because CLAUDE.md retains it on purpose. Legitimate history got a removal word next to the term, and the checker was not loosened. New weekly baseline = 0.
- 2026-09-19 | role-radar (YED-202) | the OTE floor and the non-AI-company reject are now explicit hard gates no exemption lifts; v2.3 Mid-Market rule wired into the scoring-table row. One self-caught error: the first draft invented a "MM elsewhere scores one step down" rule Alex never set. Removed before commit.
- 2026-09-19 | inbox-miner (YED-203) | 09-18 rulings wired into B2/B3 + `aliases.md`; the false "keeps bodies out of the parent" claim corrected. **Live test settled a registry conflict:** a subagent CAN call Gmail read tools (`company-researcher` ran `search_threads` successfully), so the `platform-constraints.md` row was over-broad and is corrected to say writes are parent-only and reads can be delegated when declared. One self-caught error: the first draft claimed `spine_write.py` does the GET-then-PATCH itself; corrected against the script.

### 2026-09-20 — control set + model bake-off (YED-209 steps 8/10)
- 2026-09-20 | class: harness-defect-masquerading-as-model-behaviour | count: **3 in one sitting** | action: **applied.** The first bake-off read "false-flag rate 1.0" for two arms. It was mine, not theirs: `check-refs` counted *gitignored* paths (`.claude/.state/`, `settings.local.json`, the private refs) as dangling and capped three positive controls at 0.60. Second run: a gitignored **directory** only matches `git check-ignore` with a trailing slash. Third: the pre-call budget check used `max_output_tokens` as a forecast, so `gpt-5.5` looked like a $0.52 run and that arm never executed. **Lesson: when a measurement says a model is bad, check the measurement first — three times in a row here the instrument was the defect.** The gitignored-refs bug had been silently mis-capping every judge run made in a worktree since worktrees began.
- 2026-09-20 | class: control-label-uncertainty | count: 7 excluded + 1 suspect | action: **watch → Alex's ruling.** 7 blobs resolved to BOTH pass and flag labels (judged, fixed and re-judged before the fix was committed) and were excluded from the control set rather than guessed at. An 8th, `pos-content-style-guide`, was flagged by all three arms independently with the same cited lines — better evidence the label is wrong than that three models are. Not relabelled: it disqualified every arm, and moving it post-hoc is the exact rule-bending the pre-registration guards against.
- 2026-09-20 | bake-off stage 1: **no arm qualifies** (all three fail the >1/6 false-flag rule on that one item). All three tie at 2/3 recall. A 4× price difference bought nothing measurable. Full record: `.claude/evals/controls/bakeoff-result-2026-09-20.md`. Spend $7.49 of $45.

### 2026-09-21 — third `/rigor-review` (window 2026-09-14 → 09-21; first run of the Monday cadence)
Ran with the new Step 2 null-model check and Step 2.5 tombstone check for the first time.

- 2026-09-21 | class: **harness-defect-masquerading-as-model-behaviour** | count: **4** (3 logged 09-20 + one found by this review: `controls.py` passes `--artifact-blob`, which only `openai_judge.py` knew, so **every Gemini control run failed with "unknown arg"** and the canary recorded it as a seat failure) | action: **PROPOSED — an adapter conformance test.** The class is now at 4 ≥ N=3 and every instance has the same shape: a change lands in one seat's adapter and the other silently breaks or is misjudged for it. A test that invokes every runner in `seats.json` with the documented flag set (and asserts an unknown flag exits non-zero) would have caught all four. Fixed the immediate bug: `gemini-judge.sh` now accepts `--artifact-blob` and enforces the same byte-identity check.
- 2026-09-21 | class: error-counted-as-judgement-failure | count: 1 | action: **applied (defect repair).** `run-canaries.sh` incremented `consecutive_failures` when a seat *could not run at all*, so my broken flag above would have auto-demoted the Gemini seat for my bug. Errors now track separately (`consecutive_errors`); only real judgement misses demote. Gemini's failure #1 corrected in `controls/state.json`.
- **Canaries (first scheduled run):** gemini 2/3 · openai 2/3. **Both missed the same item** (`neg-content-style-guide`, scored 0.925 and 0.76) — the control Alex relabelled yesterday after all three seats flagged it. Read honestly: on the *pre-fix* content these two now pass what they previously flagged, which is evidence the relabel was right and that this control discriminates. openai is at 2 consecutive genuine failures → **auto-demoted to shadow** (it was already shadow). gemini holds at advisory with 1.
- **Seat gate:** claude voting (n=7 scored, recall 0.71) · gemini advisory · openai shadow. **All three null-model checks report `insufficient` (n<10), not `validated`** — under the new contract no seat currently has enough paired evidence to claim it beats always-passing. That is the honest state, and it is the point of YED-212.
- **DoD waiver rate is climbing and crossed its threshold:** W33 2 → W35 6 → W37 9 → **W38 12**. Registry action is "revisit the scope test / enforcement". Judge-item waivers are only 2 of 12, so Fix 1A is holding; the growth is elsewhere (spec/PRD items, multi-session days). **PROPOSED: no change yet — re-measure next week**, because W38 spans an unusually heavy 3-session build day and one week of growth on a 4-point series is weak evidence.
- **DoD coverage: 1 of 12 build sessions ran `/dod-close`.** Exactly the denominator problem YED-212 named. Not actionable by rule yet; recorded so the "14 of 16" framing is never quoted again.
- **Low scores:** 12 non-control runs < 0.70, **all reworked and re-judged the same day** (YED-200/202/203 and the YED-209 rounds). The gate's "were they reworked?" question: yes, all.
- **Ack hygiene (new watch item):** 5 of 7 acked quorums were `agree`, and **latency is unmeasurable** — `alex_ack_at` is written as a date (`2026-09-19`), not a timestamp, so the computed 0.0h is an artifact, not a fact. Two rows store `alex_ack` as a dict rather than a string. **PROPOSED: make `alex_ack_at` a full UTC timestamp and normalise the field**, so the reflexive-agree risk YED-212 named can actually be measured. Alex did rule *against* a judge label once this week (the relabel), which is the counter-evidence, but it was recorded as a relabel and not as a disagree.
- **Container-rule grep: 32** (baseline 31). The +1 is not attributable to any file added since the baseline nor to any file this session created; most likely the 31 was measured in a worktree with slightly different contents. **Re-baselined to 32** with that caveat rather than left as a phantom rise.
- **Tombstones: 0** — the YED-200 drain is holding.

**State:** rigor **holding, with one recurring class now at threshold.** The loop did its job this week: it caught a defect (the adapter flag) that the canary would otherwise have blamed on a model, which is the same lesson as 09-20 and the reason the class needs a codified fix rather than another repair.

**Applied the same day, on Alex's rulings (2026-09-21):**
- **Adapter conformance test** (`test_adapter_contract.py`, 33 cases) — every runner in `seats.json` must accept the documented flag set, reject an unknown flag loudly, and enforce the provenance guard identically. This is the codified fix for the count-4 class. **It bit me while I wrote it:** the first version used `--print-only`, which on the Gemini adapter still calls the API, and cost **$0.11** before I killed it. So `--dry-run` (parse + guards, no network) is now part of the contract and both adapters honour it. The test is free to run.
- **Scope test tightened** (Alex chose to act rather than re-measure). 7 of the 12 waivers this window were infra builds all citing the *same* YED-129 ruling — the gate was manufacturing paperwork for a decided case, which trains people to waive. CLAUDE.md item 1 now says an ADR/in-repo spec **satisfies** the item for infra work; a waiver means one thing only: *no spec of any kind existed before the code.* Separately, `--record` writes a non-waiver note (`type:"record"`), since 2 of the 12 were my YED-190 redemption rows inflating the count.
- **Ack hygiene made measurable** — `alex_ack` normalised to a string (2 rows held a dict), `alex_ack_at` upgraded to a full UTC timestamp (6 files), and the judge-build skill now states the contract with the reason. Existing date-only stamps are marked `date-only (backfilled)` rather than silently treated as midnight.
- **Gap found, not yet fixed:** the spend ledger covers OpenAI only. **Gemini calls are untracked**, so the "judge API spend" registry row measures part of the truth. Filed as a follow-up rather than patched mid-review.
